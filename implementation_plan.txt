E-Commerce Agent — Complete Strategy & Implementation PlanBackground & What We Already HaveYour existing MCP Auditor is a 4-service architecture:Service	Port	Rolebackend `:8000`	FastAPI + FastMCP	Shopify Admin API wrapper (products, collections, pages, blogs, articles, policies) exposed as MCP toolsmcp-service `:8001`	FastMCP	Storefront crawler (crawl, metadata, images, links) exposed as MCP toolsagent-service `:8002`	FastAPI	Claude-powered agentic loop — routes tool calls to backend & crawler MCP serversfrontend `:3000`	Next.js	Audit dashboard UIKey assets we reuse:ShopifyClient — GraphQL paginator for Shopify Admin APIToolRouter — MCP multi-server tool routingAgentic loop pattern — Claude tool-use loop with iteration limitsShopify API credentials (shop domain, access token, API version `2025-01`)Anthropic API key + Claude Sonnet 4.6 model---Target Capabilities#	Capability	What It Does	Data Source1	Product Discovery & MoodBoard	Natural-language product search ("show me cozy winter sweaters under $50") + visual moodboard grid	Shopify Admin API (products with images, prices, tags, variants)2	Product Recommendations	"Customers who liked X also liked Y", complementary items, upsells	Shopify product data + AI semantic similarity3	FAQ Answering	Answer questions using store policies, product details, and page content	Shopify pages, policies, product descriptions → Vector store4	Order Tracking	Look up order status by order number + email/phone	Shopify Admin API (Orders)5	Ticket Creation	Create a support ticket when the agent can't resolve an issue	Shopify Admin API (DraftOrders notes / metafields) or external helpdesk webhook6	Chat UI	Conversational interface for all of the above	New Next.js chat page---High-Level Architecture```mermaidgraph TB    subgraph "Frontend (Next.js :3000)"        ChatUI["Chat UI<br/>Conversational Interface"]        MoodBoard["MoodBoard View<br/>Product Grid + Images"]        OrderView["Order Status View"]    end    subgraph "ecom-agent-service (:8003)"        Router["Intent Router<br/>(Claude Tool-Use Loop)"]        ConvMgr["Conversation Manager<br/>(Session Memory)"]    end    subgraph "ecom-mcp-service (:8004)"        ProductSearch["search_products<br/>MCP Tool"]        ProductRecommend["get_recommendations<br/>MCP Tool"]        FAQAnswer["search_faq<br/>MCP Tool"]        OrderTrack["track_order<br/>MCP Tool"]        TicketCreate["create_ticket<br/>MCP Tool"]        MoodBoardGen["generate_moodboard<br/>MCP Tool"]    end    subgraph "Existing Services"        Backend["backend (:8000)<br/>Shopify Admin MCP"]        MCPService["mcp-service (:8001)<br/>Crawler MCP"]    end    subgraph "Data Layer"        VectorDB["ChromaDB / Qdrant<br/>Product Embeddings<br/>FAQ Embeddings"]        SessionStore["Redis / In-Memory<br/>Conversation State"]    end    subgraph "External"        ShopifyAPI["Shopify Admin API"]        ShopifySF["Shopify Storefront API"]    end    ChatUI -->|WebSocket / SSE| Router    Router --> ConvMgr    Router -->|MCP tool calls| ProductSearch    Router -->|MCP tool calls| ProductRecommend    Router -->|MCP tool calls| FAQAnswer    Router -->|MCP tool calls| OrderTrack    Router -->|MCP tool calls| TicketCreate    Router -->|MCP tool calls| MoodBoardGen    Router -->|MCP tool calls| Backend    MoodBoard -.->|renders| ProductSearch    OrderView -.->|renders| OrderTrack    ProductSearch --> VectorDB    ProductRecommend --> VectorDB    FAQAnswer --> VectorDB    OrderTrack --> ShopifyAPI    TicketCreate --> ShopifyAPI    MoodBoardGen --> ShopifySF    ProductSearch --> ShopifyAPI    FAQAnswer --> ShopifyAPI```---User Review Required> [!IMPORTANT]> **New project or extend existing?** This plan creates a **new project** (`Ecom_Agent`) alongside your existing `MCP_Auditor`. We'll copy and adapt shared utilities (ShopifyClient, ToolRouter) rather than modifying the auditor. If you'd prefer everything inside the same repo, let me know.> [!IMPORTANT]> **Ticket system target:** For ticket creation, we have two options:> 1. **Shopify-native** — store tickets as Order notes or metafields (simpler, no external dependency)> 2. **External helpdesk** — webhook to Zendesk / Freshdesk / Gorgias (more powerful, requires API key)>> Which do you prefer?> [!WARNING]> **Shopify API scopes needed:** The E-commerce agent requires additional Shopify Admin API scopes beyond what the SEO auditor uses:> - `read_orders` — for order tracking> - `read_customers` — for customer lookup during order tracking> - `write_draft_orders` or `write_metafields` — if using Shopify-native ticket system> - Existing scopes: `read_products`, `read_content`, etc.>> Please confirm your Shopify app has (or can be granted) these scopes.---Open Questions> [!IMPORTANT]> **Vector store choice:** ChromaDB is zero-config and embeds locally (good for dev). Qdrant is production-grade. Which do you prefer, or should I start with ChromaDB and plan for Qdrant migration?> [!IMPORTANT]> **Streaming responses:** Do you want the chat to stream tokens (SSE/WebSocket) like ChatGPT, or is a simple request-response OK for now?> [!NOTE]> **Product images for MoodBoard:** Your current `get_products` query fetches `altText` but not `src` for images. We'll need to extend the GraphQL query to include image URLs + pricing data for the moodboard display.---Proposed ChangesPhase 1: Data Foundation & MCP ToolsThis phase builds the new MCP server with all 6 tool capabilities, a vector store for semantic search, and extends the Shopify client.---[NEW] `ecom-mcp-service/` — E-Commerce MCP Tool Server (Port 8004)The core tool server exposing all e-commerce capabilities as MCP tools that the agent can call.[NEW] `ecom-mcp-service/app/shopify_client.py`Extended ShopifyClient (copied from shopify_client.py) with:Products with full data — images with `src` + `altText`, `priceRangeV2`, `tags`, `productType`, `vendor`, `variants` (size/color)Orders lookup — `orders(query: "name:#1234 AND email:foo@bar.com")` with fulfillment status, tracking URL, line itemsCustomers lookup — basic customer search for order verificationMetafield write — for ticket storage (if Shopify-native)```graphql# New: Products with images, pricing, and variantsquery ProductsFull($first: Int!, $after: String) {  products(first: $first, after: $after, query: "status:active") {    edges {      node {        id, title, handle, descriptionHtml, productType, vendor, tags        seo { title, description }        priceRangeV2 {          minVariantPrice { amount, currencyCode }          maxVariantPrice { amount, currencyCode }        }        images(first: 5) {          edges { node { url, altText, width, height } }        }        variants(first: 10) {          edges { node { title, price, availableForSale, selectedOptions { name, value } } }        }      }    }    pageInfo { hasNextPage, endCursor }  }}# New: Order trackingquery OrderLookup($query: String!) {  orders(first: 5, query: $query) {    edges {      node {        id, name, createdAt, displayFinancialStatus, displayFulfillmentStatus        totalPriceSet { shopMoney { amount, currencyCode } }        fulfillments {          trackingInfo { number, url, company }          status        }        lineItems(first: 20) {          edges { node { title, quantity, variant { image { url } } } }        }        shippingAddress { city, province, country }      }    }  }}```[NEW] `ecom-mcp-service/app/vector_store.py`Vector store manager using ChromaDB for:Product embeddings — title + description + tags + product type → embeddingFAQ embeddings — page content + policy content + blog content → embeddingSync function — `sync_products()` and `sync_faq_content()` that pull from Shopify and upsert embeddingsSearch functions — `search_products(query, filters, top_k)` and `search_faq(query, top_k)`Uses `text-embedding-3-small` (OpenAI) or Claude's built-in embeddings. Will add a config toggle.[NEW] `ecom-mcp-service/app/server.py`FastMCP server exposing 6 tools:```python@mcp.toolasync def search_products(query: str, max_results: int = 10,                           min_price: float = None, max_price: float = None,                          product_type: str = None) -> list[dict]:    """Search products by natural language query with optional filters.    Returns products with images, prices, and relevance scores."""@mcp.tool  async def get_recommendations(product_id: str, max_results: int = 5) -> list[dict]:    """Get product recommendations similar to or complementary to a given product.    Uses semantic similarity on product embeddings."""@mcp.toolasync def generate_moodboard(query: str, max_items: int = 8) -> dict:    """Generate a visual moodboard for a theme/aesthetic query.    Returns curated product set with images optimized for grid display."""@mcp.toolasync def search_faq(question: str) -> list[dict]:    """Search store FAQs, policies, and content pages for answers.    Returns relevant content chunks with source attribution."""@mcp.toolasync def track_order(order_number: str, email: str = None,                        phone: str = None) -> dict:    """Look up order status by order number + customer verification.    Returns order status, tracking info, and line items."""@mcp.toolasync def create_ticket(customer_email: str, subject: str,                          description: str, priority: str = "normal",                         order_number: str = None) -> dict:    """Create a support ticket when the agent cannot resolve an issue.    Returns ticket ID and confirmation."""```[NEW] `ecom-mcp-service/app/indexer.py`Background indexing service:On startup (or on-demand via API), pulls all products, pages, policies, blogs from ShopifyChunks text content, generates embeddings, upserts to ChromaDBStores product metadata (price, images, tags) alongside embeddings for filteringIncremental sync via Shopify webhooks (future) or polling[NEW] `ecom-mcp-service/requirements.txt````fastmcphttpxchromadbopenai  # for embeddings (or use anthropic)beautifulsoup4pydantic-settings```---Phase 2: Agent Service (Conversational Loop)[NEW] `ecom-agent-service/` — E-Commerce Agent (Port 8003)Adapted from agent-service but redesigned for multi-turn conversation.[NEW] `ecom-agent-service/app/services/agent.py`Key differences from the SEO auditor agent:Multi-turn conversation — maintains message history across requests (not one-shot)Session management — each chat session gets a UUID, messages stored in memory (Redis for production)Streaming — SSE endpoint for real-time token streamingIntent-aware system prompt — handles product discovery, FAQ, orders, tickets in one agent```pythonSYSTEM_PROMPT = """You are a friendly, knowledgeable e-commerce shopping assistant for a Shopify store.You can help customers with:1. **Product Discovery** — Search for products, answer questions about products, suggest items2. **MoodBoard** — Create visual moodboards for aesthetic/theme-based shopping3. **Product Recommendations** — Suggest similar or complementary products4. **FAQ Answering** — Answer questions about shipping, returns, policies, sizing, etc.5. **Order Tracking** — Look up order status (requires order number + email)6. **Support Tickets** — Create a support ticket if you can't resolve an issueGuidelines:- Be conversational, warm, and helpful- When showing products, always include images and prices- For order tracking, always verify customer identity (order # + email)- If you can't resolve something, offer to create a support ticket- Never fabricate product information — only share what the tools return- Format product results as structured cards the UI can render- When creating moodboards, select products that match the aesthetic theme"""```[NEW] `ecom-agent-service/app/services/mcp_tools.py`Extended ToolRouter that connects to:`ecom-mcp-service` (:8004) — all 6 new toolsOptionally `backend` (:8000) — reuse existing Shopify admin tools for raw data[NEW] `ecom-agent-service/app/services/session.py`Session manager:```pythonclass SessionManager:    async def create_session(store_id: str) -> str  # returns session_id    async def get_messages(session_id: str) -> list[dict]    async def append_message(session_id: str, role: str, content: Any)    async def clear_session(session_id: str)```[NEW] `ecom-agent-service/app/api/routes/chat.py````pythonPOST /chat          # Send message, get response (non-streaming)POST /chat/stream   # Send message, get SSE streamPOST /sessions      # Create new chat session  GET  /sessions/{id} # Get session historyDELETE /sessions/{id}POST /index/sync    # Trigger re-indexing of products/FAQ```---Phase 3: Chat Frontend[MODIFY] `frontend/` — Add Chat Interface[NEW] `frontend/app/chat/page.tsx`Full-screen conversational chat page with:Message bubbles (user + assistant)Product card rendering (image grid, price, "View Product" link)MoodBoard grid view (Pinterest-style masonry layout)Order status cardTicket confirmation cardTyping indicator during streamingQuick action buttons ("Track Order", "Browse Products", "Help")[NEW] `frontend/components/chat/````ChatWindow.tsx      — Main chat container with message list + inputMessageBubble.tsx   — Individual message renderingProductCard.tsx     — Rich product display (image, title, price, link)MoodBoardGrid.tsx   — Masonry grid of product imagesOrderStatusCard.tsx — Order tracking displayTicketCard.tsx      — Support ticket confirmationQuickActions.tsx    — Suggested action chips```[MODIFY] `frontend/app/page.tsx`Add navigation link to the new Chat page from the existing dashboard.---Phase 4: Polish & Production ReadinessData PipelineWebhook-based indexing — Register Shopify webhooks for `products/update`, `products/create`, `products/delete` to keep vector store in syncScheduled full sync — Daily cron job to re-index everythingPerformanceCaching — Cache product search results (5-min TTL) to reduce API callsEmbedding batching — Batch embedding requests for cost efficiencyConnection pooling — Reuse httpx clients across requestsSecurityRate limiting — Per-session rate limits on chat endpointInput sanitization — Guard against prompt injection in customer messagesOrder verification — Require both order # and email before showing order details---Project Structure (New Files)```Ecom_Agent/├── ecom-mcp-service/           # Port 8004│   ├── app/│   │   ├── __init__.py│   │   ├── server.py           # FastMCP with 6 tools│   │   ├── shopify_client.py   # Extended Shopify GraphQL client│   │   ├── vector_store.py     # ChromaDB embeddings manager│   │   ├── indexer.py          # Product/FAQ indexing pipeline│   │   └── config.py           # Settings│   ├── .env│   └── requirements.txt│├── ecom-agent-service/         # Port 8003│   ├── app/│   │   ├── __init__.py│   │   ├── main.py             # FastAPI app│   │   ├── api/│   │   │   └── routes/│   │   │       └── chat.py     # Chat + session endpoints│   │   ├── core/│   │   │   └── config.py       # Settings│   │   ├── services/│   │   │   ├── agent.py        # Multi-turn Claude loop│   │   │   ├── mcp_tools.py    # Tool router│   │   │   └── session.py      # Session manager│   │   └── models/│   │       └── schemas.py      # Pydantic models│   ├── .env│   └── requirements.txt│├── frontend/                   # Extended existing frontend│   ├── app/│   │   └── chat/│   │       └── page.tsx        # Chat page│   └── components/│       └── chat/│           ├── ChatWindow.tsx│           ├── MessageBubble.tsx│           ├── ProductCard.tsx│           ├── MoodBoardGrid.tsx│           ├── OrderStatusCard.tsx│           ├── TicketCard.tsx│           └── QuickActions.tsx```---Environment Variables (New)```env# ecom-mcp-service/.envSHOPIFY_SHOP_DOMAIN=your-store.myshopify.comSHOPIFY_ACCESS_TOKEN=shpat_xxxxxSHOPIFY_API_VERSION=2025-01OPENAI_API_KEY=sk-xxx          # for embeddings (or use ANTHROPIC)CHROMA_PERSIST_DIR=./data/chromaMCP_PORT=8004# ecom-agent-service/.envANTHROPIC_API_KEY=sk-ant-xxxANTHROPIC_MODEL=claude-sonnet-4-6ECOM_MCP_URL=http://localhost:8004/mcpBACKEND_MCP_URL=http://localhost:8000/mcp   # optional: reuse existingMAX_AGENT_ITERATIONS=15SESSION_TTL_MINUTES=60AGENT_PORT=8003```---Phased RolloutPhase	Scope	Estimated EffortPhase 1	`ecom-mcp-service` — Shopify client extensions, vector store, all 6 MCP tools, indexer	Core data layer — build firstPhase 2	`ecom-agent-service` — Multi-turn agent loop, session management, chat API	Agent brainPhase 3	Frontend chat UI — Chat window, product cards, moodboard grid, order cards	User-facingPhase 4	Webhooks, caching, rate limiting, production hardening	Polish---Verification PlanAutomated Tests```bash# Phase 1: Test MCP tools individuallycd ecom-mcp-servicepython -m pytest tests/ -v# Phase 2: Test agent conversation flowcd ecom-agent-service  python -m pytest tests/ -v# Integration: Test full flowpython -m pytest tests/integration/ -v```Manual VerificationProduct Discovery: Ask "show me red dresses under $100" → verify relevant products returned with imagesMoodBoard: Ask "create a cozy autumn living room moodboard" → verify image grid rendersFAQ: Ask "what's your return policy?" → verify answer matches actual store policyOrder Tracking: Use a real order number + email → verify status matches Shopify adminTicket Creation: Ask to create a ticket → verify it appears in Shopify/helpdeskMulti-turn: Have a full conversation switching between capabilities → verify context maintainedBrowser TestingTest chat UI on desktop and mobile viewportsVerify product cards render images correctlyVerify moodboard masonry layoutTest streaming (typing indicator, progressive rendering)